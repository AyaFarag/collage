@extends("admin.layout.app")

@section("navbar")
  <h4>Update a schedule</h4>
@stop

@section("content")
  <div class="padded-container">
    <div class="card">
      <div class="card-content">
        @component("admin.components.form", [
          "method" => "put",
          "action" => route("admin.semester-session.schedule.update", [$semester_session, $schedule -> id])
        ])
          @include("admin.schedule.form")
          @include("admin.components.button", [
            "icon"  => "replay",
            "label" => "update"
          ])
        @endcomponent
      </div>
    </div>
  </div>
@stop